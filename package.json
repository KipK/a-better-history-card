{
  "name": "a-better-history-card",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build && npm run build:history-lib",
    "build:history-lib": "node scripts/build-ha-better-history.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kipk/ha-better-history": "file:../ha-better-history",
    "@kipk/load-ha-components": "^1.0.3",
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.0.10"
  }
}